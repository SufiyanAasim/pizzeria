# Examples

## `menu-import.json`

A sample payload in the shape `scripts/seed.ts` expects, useful if you
want to seed Turso from an external menu source instead of
`src/lib/menu-data.ts`. Adapt `scripts/seed.ts` to read from a file
like this if the menu ever needs to be editable without a code change.
